{% extends './layout.njk' %}

{% block content %}
    <div class="lista-chefs">
        {% for chef in chefs  %}
        <div class="chefs-admin">
            <div>
                <img src="{{chef.img}}" alt="">
            </div>
            <div class="info-chef-site">
                <h4>{{chef.name}}</h4>
                {% if chef.total_recipes > 1 %}
                    <p>{{chef.total_recipes}} receitas</p>
                {% else %}
                    <p>{{chef.total_recipes}} receita</p>
                {% endif %}
            </div>
        </div>
        {% endfor %}
    </div>
{% endblock %}